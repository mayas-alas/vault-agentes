export class BrowserVoiceAdapter{
constructor(){this.active=false;this.frame=0;this.run=0}
async start({onLevel,onText,onState}){const run=++this.run;this.active=true;onState('listening');try{const stream=await navigator.mediaDevices.getUserMedia({audio:true});if(!this.active||run!==this.run){stream.getTracks().forEach(track=>track.stop());return}this.stream=stream;this.context=new AudioContext();const analyser=this.context.createAnalyser();analyser.fftSize=128;this.context.createMediaStreamSource(stream).connect(analyser);const data=new Uint8Array(analyser.frequencyBinCount),tick=()=>{if(!this.active||run!==this.run)return;analyser.getByteFrequencyData(data);onLevel(data.reduce((a,b)=>a+b,0)/data.length/110);this.frame=requestAnimationFrame(tick)};tick()}catch{if(!this.active||run!==this.run)return;this.synthetic(onLevel,run)}if(!this.active||run!==this.run)return;const Recognition=window.SpeechRecognition||window.webkitSpeechRecognition;if(Recognition){this.recognition=new Recognition();this.recognition.lang='es-MX';this.recognition.continuous=true;this.recognition.interimResults=true;this.recognition.onresult=event=>{const result=event.results[event.results.length-1];onText(result[0].transcript,result.isFinal)};this.recognition.onerror=()=>{onState('fallback');setTimeout(()=>this.active&&run===this.run&&onText('Quiero ordenar mi agenda y responder mejor a mis clientes',true),900)};this.recognition.onend=()=>{if(this.active&&run===this.run)try{this.recognition.start()}catch{}};this.recognition.start()}else setTimeout(()=>this.active&&run===this.run&&onText('Quiero ordenar mi agenda y responder mejor a mis clientes',true),2200)}
synthetic(onLevel,run){const tick=()=>{if(!this.active||run!==this.run)return;onLevel(.24+Math.random()*.42);this.frame=requestAnimationFrame(tick)};tick()}
stop(){this.active=false;this.run++;cancelAnimationFrame(this.frame);this.stream?.getTracks().forEach(track=>track.stop());this.stream=null;this.recognition?.stop();this.recognition=null;this.context?.close();this.context=null}
speak(text,onEnd=()=>{}){if(!('speechSynthesis'in window)){onEnd();return}speechSynthesis.cancel();const utterance=new SpeechSynthesisUtterance(text);utterance.lang='es-MX';utterance.rate=.97;utterance.pitch=.94;utterance.onend=onEnd;speechSynthesis.speak(utterance)}
}
