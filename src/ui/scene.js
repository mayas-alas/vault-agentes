import*as THREE from'three';
export class PulseScene{constructor(canvas,{compact=false}={}){this.canvas=canvas;this.energy=.15;this.scene=new THREE.Scene();this.camera=new THREE.PerspectiveCamera(42,1,.1,100);this.camera.position.z=compact?5.4:6.5;this.renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true});this.renderer.setPixelRatio(Math.min(devicePixelRatio,1.7));this.group=new THREE.Group();this.scene.add(this.group);const count=compact?900:1300,positions=new Float32Array(count*3);for(let i=0;i<count;i++){const r=1.5+Math.random()*1.15,a=Math.random()*Math.PI*2,b=Math.acos(2*Math.random()-1);positions[i*3]=r*Math.sin(b)*Math.cos(a);positions[i*3+1]=r*.7*Math.sin(b)*Math.sin(a);positions[i*3+2]=r*Math.cos(b)}const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(positions,3));this.points=new THREE.Points(geometry,new THREE.PointsMaterial({color:0xa8f0c3,size:compact?.022:.018,transparent:true,opacity:.65,blending:THREE.AdditiveBlending}));this.group.add(this.points);for(let i=0;i<4;i++){const ring=new THREE.Mesh(new THREE.TorusGeometry(1.45+i*.23,.006,5,170),new THREE.MeshBasicMaterial({color:i%2?0xd8ff8f:0x6eb28a,transparent:true,opacity:.18}));ring.rotation.set(Math.random()*2,Math.random()*2,Math.random()*2);this.group.add(ring)}this.core=new THREE.Mesh(new THREE.IcosahedronGeometry(.58,2),new THREE.MeshBasicMaterial({color:0x9fe9ba,wireframe:true,transparent:true,opacity:.4}));this.group.add(this.core);this.clock=new THREE.Clock();this.resize();new ResizeObserver(()=>this.resize()).observe(canvas);this.animate()}setEnergy(value){this.energy=Math.max(.08,Math.min(value,1.4))}resize(){const box=this.canvas.getBoundingClientRect();if(!box.width||!box.height)return;this.renderer.setSize(box.width,box.height,false);this.camera.aspect=box.width/box.height;this.camera.updateProjectionMatrix()}animate(){const time=this.clock.getElapsedTime(),pulse=1+Math.sin(time*2.2)*.025+this.energy*.11;this.group.rotation.y=time*.08;this.group.rotation.x=Math.sin(time*.12)*.2;this.points.rotation.z=-time*.035;this.core.scale.setScalar(pulse);this.core.rotation.x=time*.22;this.core.rotation.y=time*.18;this.group.children.filter(x=>x.geometry?.type==='TorusGeometry').forEach((ring,index)=>ring.rotation.z+=.0008*(index+1)*(1+this.energy));this.renderer.render(this.scene,this.camera);requestAnimationFrame(()=>this.animate())}}
